<% content_for :title, "Édition de l'équipe" %>

<div class="max-w-7xl mx-auto p-6">
  <h1 class="font-bold text-4xl mb-6 text-center">Édition de l'équipe : <%= @team.name %></h1>

  <%= render "form", team: @team %>

</div>
