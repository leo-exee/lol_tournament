<% content_for :title, "Tous les joueurs" %>

<div class="max-w-7xl mx-auto p-6">

  <div class="flex justify-between items-center mb-6">
    <h1 class="font-bold text-3xl text-gray-800">Tous les joueurs</h1>
    <% if admin? %>
      <a href="/players/new" class="rounded-md px-4 py-2 bg-yellow-400 hover:bg-yellow-500 text-black font-medium flex items-center transition duration-200">
        <i class="fas fa-plus pr-2"></i>
        Créer un joueur
      </a>
    <% end %>
  </div>

  <% if @players.any? %>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @players.each do |player| %>
        <%= link_to player_path(player), class: "block bg-white p-4 rounded-xl shadow hover:shadow-lg transition duration-200" do %>
          <div class="flex items-center space-x-4">
            <%= image_tag(player.role_icon, class: "w-14 h-14 rounded-full object-cover") %>
            <div>
              <h2 class="text-lg font-semibold text-gray-800 mb-1">
                <%= player.first_name %> <%= player.last_name %>
              </h2>
              <p class="text-sm text-gray-500">🤝 Équipe : <%= player.team.name %></p>
              <p class="text-sm text-gray-500">🎭 Rôle : <%= player.role %></p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <p class="text-center text-gray-500 mt-10">Aucun joueur trouvé.</p>
  <% end %>

</div>
